export function splitStr(str, positionGet, char) {
    if (str === undefined || str === null) {
      return '';
    }
    return str.split(char)[positionGet];
}
const DataTypeDic=['text','number']
export function checkContain(str) {
    for(let item of DataTypeDic){
        
    }
    return false;
}